def update
  @${1:model_instance_name} = ${2:model_name}.find(params[:id])
  if @$1.update_attributes(params[:$1])
	flash[:notice] = '$2 was successfully updated.'
	redirect_to(@$1)
  else
	render :action => "edit"
  end
end${3}
